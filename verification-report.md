# patrol/reliability 接口验证报告

> 生成时间:2026-07-30
> 验证人:自动脚本 `loop_call.py`

---

## 一、验证目标

验证 `patrol/reliability` 接口在**固定入参**下的:
1. HTTP 连通性与响应延迟;
2. 返回数据的**正确性 / 一致性**(相同入参是否返回相同结果)。

---

## 二、被测接口

- **URL**:`POST http://192.168.8.105:80/p2/v1/dr/css/proto/patrol/reliability`
- **Headers**:
  - `User-Agent: 192.168.40.135`
  - `Content-Type: application/json`
- **Body**(全程固定):
  ```json
  {
    "hostId": "erzz8m3uvdskjk",
    "statsType": "2",
    "beginTime": "2026-07-29 00:00:00",
    "endTime": "2026-07-29 23:59:59",
    "stationCode": "160110102701010000"
  }
  ```

**返回结构示例**:
```json
{
  "code": 200,
  "data": {
    "percent": "1.719%",
    "statsType": 2,
    "totalNum": 698,
    "validNum": 12
  },
  "message": "查询成功",
  "timestamp": "2026-07-30 16:54:38"
}
```

---

## 三、验证方式

| 项 | 配置 |
|---|---|
| 工具 | `loop_call.py`(`urllib` + `threading`,纯标准库) |
| 总请求数 | 100 |
| 并发 | 2 线程(每线程 50 次) |
| 超时 | 60s |

**断言规则**(逐条判定):
1. HTTP 状态码 = 200,且响应体可解析为 JSON;
2. 业务 `code == 200`;
3. **`$.data.percent == "1.719%"`** → 判定 **PASS**;任一不满足 → **FAIL**。

> ⚠️ **字段说明**:带 `%` 的是 `data.percent`;`data.statsType` 是整数 `2`,不会是百分比。因此断言目标字段为 `percent`。

**失败分类**:`HTTP_ERROR`(网络/超时)/ `BAD_JSON`(响应非 JSON)/ `BIZ_CODE_*`(业务码非 200)/ `ASSERT_FAIL`(`percent` 值不符)。

**结果落盘**:每次请求的关键字段写入 `success.json`(PASS 明细)与 `fail.json`(FAIL 明细,含原因、期望值、实际值)。

---

## 四、执行结果

### 轮次 ① — 纯连通性压测(仅看 HTTP 200,无数据断言)

| 指标 | 数值 |
|---|---|
| HTTP 200 | 100 / 100 |
| 失败 | 0 |
| 平均延迟 | 388.7 ms |
| P95 延迟 | 472.9 ms |
| 最小 / 最大 | 344.4 / 546.3 ms |
| 吞吐 | 5.12 req/s |
| 总耗时 | 19.53 s |

**结论**:HTTP 层连通稳定,无超时、无网络错误。

---

### 轮次 ② — 数据正确性断言(期望 `percent = 1.719%`)

| 指标 | 数值 |
|---|---|
| **PASS** | **98** |
| **FAIL** | **2** |
| 总耗时 | 19.72 s |

**失败明细**:

| idx | 实际 percent | totalNum | validNum | 耗时 | 原因 |
|---|---|---|---|---|---|
| 28 | `0.000%` | 118 | 0 | 458 ms | ASSERT_FAIL |
| 92 | `5.983%` | 117 | 7 | 561 ms | ASSERT_FAIL |

(正常返回:`totalNum=698, validNum=12 → 12/698 = 1.719%`)

---

### 轮次 ③ — 原参数复跑断言(期望 `percent = 1.719%`)

| 指标 | 数值 |
|---|---|
| **PASS** | **100** |
| **FAIL** | **0** |
| 总耗时 | 18.64 s |

**结论**:本轮全部通过,无异常。

---

## 五、关键发现 ⚠️

**接口返回数据存在偶发性不一致**:在入参完全相同的情况下,100 次请求出现了 2 次结果偏差,且偏差**不在单个字段**,而是底层统计量整体变化:

| | 正常 | 异常(idx28) | 异常(idx92) |
|---|---|---|---|
| totalNum | 698 | 118 | 117 |
| validNum | 12 | 0 | 7 |
| percent | 1.719% | 0.000% | 5.983% |

`percent = validNum / totalNum`,即百分比差异源自 `totalNum / validNum` 的波动(正常 698 vs 异常 117/118,差近一个数量级)。该问题在轮次②复现、轮次③未复现,**属偶发**。

**可能成因(待后端排查)**:
- 后端实时查询未做缓存,数据源/快照在请求间变动;
- 多节点分片部署,各节点数据不统一;
- 查询窗口存在采样 / 分页差异。

---

## 六、产出文件

| 文件 | 说明 |
|---|---|
| `loop_call.py` | 验证脚本(顶部 `TOTAL / THREADS / FIELD / EXPECT` 可调) |
| `success.json` | 最近一轮 PASS 明细(100 条) |
| `fail.json` | 最近一轮 FAIL 明细(0 条,轮次②为 2 条,已被覆盖) |
| `verification-report.md` | 本报告 |

---

## 七、复现建议

当前偶发率约 **1%~2%**(两轮共 200 次,出现 2 次异常)。如需提高复现概率:
- 提升并发(如 5 / 10 线程);
- 连续多轮跑并汇总(脚本可改为外层循环);
- 在不同时段重跑(排查是否与后端数据刷新周期相关)。
